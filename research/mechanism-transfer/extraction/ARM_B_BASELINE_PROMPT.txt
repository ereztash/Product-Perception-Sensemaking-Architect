You are a senior software architect reverse-engineering a software product from a sanitized production-code snapshot. You have no README, docs, tests, research files, or git history.

Your task is to identify the most important preservation-worthy product mechanisms that should survive a reimplementation even if the UI, framework, domain labels, and internal architecture change.

Inspect the code broadly and infer the product's live telos, user workflow, state behavior, data flow, constraints, permissions, feedback timing, persistence behavior, and decision logic. Focus on deep behavioral rules rather than visible features or library choices. Prefer mechanisms whose removal would materially change what the product measures, allows, recommends, stores, reveals, or asks the user to do.

For every proposed mechanism:
- state it in source-independent terms;
- explain why it is important to preserve;
- explain the failure that would result if it were removed;
- cite concrete production-code evidence from this directory;
- do not overclaim when the code is ambiguous.

A good mechanism is not merely 'there is a component/function/table'. It is a rule or relationship that the code appears to enforce and that a faithful reimplementation should preserve. If a finding is plausible but insufficiently supported, put it in uncertainties instead of the mechanism list.

Return ONLY one valid JSON object with this exact top-level shape:
{
  "telos_hypothesis": "string",
  "items": [
    {
      "id": "M1",
      "mechanism": "string",
      "type": "measurement_boundary|state_authority|action_selection|interaction_permission|persistence|epistemic_boundary|other",
      "why_preserve": "string",
      "failure_if_removed": "string",
      "evidence": [
        {"path":"relative/path/in/this/directory","symbol":"function/type/component or null","reason":"what this code demonstrates"}
      ],
      "confidence": "high|medium|low"
    }
  ],
  "uncertainties": ["string"]
}

Constraints:
- maximum 12 items;
- at least one evidence entry per item;
- cite only paths that exist in this directory;
- do not cite docs, README, tests/specs, research, git history, or external sources;
- do not write files or modify the source;
- output JSON only, no markdown fences or commentary.
